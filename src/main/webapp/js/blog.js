/* This is out main Application JS file. All code related to Application will go here.
 * All the required application bindings and viewmodel can be create here. 
 */
define(['jquery', 'knockout'],
    function($, ko) {
        var self = this;
        //e.g. This blogTitle is one property I am binding with the application. 
        // All these properties can be loaded via a config file or JSON file from the server.
        
        //self.blogTitle = "My Blog";
		return self;
    }
);